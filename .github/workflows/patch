+cmake_minimum_required(VERSION 3.15) +project(ROT LANGUAGES C CXX) + +option(BUILD_FULL_ROT "Build full ROT kernel (may require additional deps)" OFF) + +add_subdirectory(quantum_backend) +add_subdirectory(test) + +if(BUILD_FULL_ROT)

message(STATUS "BUILD_FULL_ROT=ON: attempting to add ROT kernel build (best-effort)")
file(GLOB ROT_SOURCES ".c" ".cpp")
if(ROT_SOURCES)
   add_executable(rot_kernel ${ROT_SOURCES})
   target_compile_features(rot_kernel PRIVATE c_std_11)
   target_link_libraries(rot_kernel PRIVATE rot_runtime)
else()
   message(WARNING "No top-level C/C++ sources found for full ROT build")
